services:
  - type: web
    name: startup-survival-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    pythonVersion: "3.11.0"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
